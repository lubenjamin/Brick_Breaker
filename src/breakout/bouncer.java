package breakout;

import javafx.scene.image.Image;

/*
- bouncer is a game element
- passes in an image that will be displayed as the ball
 */
public class bouncer extends element{
    public bouncer(Image image){
        super(image, 0);
    }
}
